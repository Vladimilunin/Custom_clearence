# Руководство по облачному развертыванию

## Файлы конфигурации

Для облачного деплоя используются следующие файлы:

### backend/.env.cloud
Содержит **реальные production credentials**. Этот файл:
- ✅ Уже создан с вашими реальными данными
- ✅ Добавлен в `.gitignore` (не будет закоммичен)
- ✅ Автоматически загружается скриптами деплоя

**⚠️ ВАЖНО:** Не удаляйте и не коммитьте этот файл в Git!

## Быстрый деплой

### Backend на Cloud Run

**Windows:**
```powershell
cd backend
.\deploy_cloud_run.ps1
```

**Linux/Mac:**
```bash
cd backend
chmod +x deploy_cloud_run.sh
./deploy_cloud_run.sh
```

Скрипт автоматически:
1. Загрузит credentials из `.env.cloud`
2. Соберет Docker image
3. Залить в Google Container Registry
4. Задеплоит на Cloud Run
5. Выведет URL сервиса

### Frontend на Vercel

**Windows:**
```powershell
.\scripts\deploy-frontend-vercel.ps1
```

**Linux/Mac:**
```bash
./scripts/deploy-frontend-vercel.sh
```

После деплоя backend скопируйте Service URL и установите в Vercel:
```
NEXT_PUBLIC_API_URL=<cloud-run-url>
```

## Что находится в .env.cloud

✅ **Database:** Neon PostgreSQL connection  
✅ **Storage:** Cloudflare R2 credentials  
✅ **GCP:** Project ID  
📝 **AI Keys:** Нужно добавить GEMINI_API_KEY и OPENROUTER_API_KEY

## Обновление credentials

Просто отредактируйте `backend/.env.cloud` и запустите деплой снова.

## Проверка деплоя

После успешного деплоя:
1. Откройте `<service-url>/docs` - должна открыться Swagger UI
2. Протестируйте эндпоинт `/` - должен вернуть `{"Hello":"World"}`
3. Проверьте что изображения загружаются в R2

## Troubleshooting

**Проблема:** Скрипт не находит .env.cloud  
**Решение:** Убедитесь что вы запускаете скрипт из директории `backend/`

**Проблема:** Недостающие переменные  
**Решение:** Добавьте их в `backend/.env.cloud`

**Проблема:** Ошибка авторизации GCP  
**Решение:** 
```bash
gcloud auth login
gcloud config set project tamozh-backend-479110
```
